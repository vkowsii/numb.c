#include<stdio.h>
int main()
{
    char ch;
    printf("enter the character");
    scanf("%d",&ch);
    if(ch<=a&&ch<=z)&&(ch<=A&&ch<=Z)
    {
        printf("character");
    }
    printf("not character");
}
